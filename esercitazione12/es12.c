#include <stdlib.h>
#include <stdio.h>
#include "es12.h"

/* Non è obbligatorio implementare tutte queste funzioni per svolgere gli esercizi,
 * ma alcune possono essere particolarmente utili; nel caso decideste di non usarle,
 * potete tranquillamente rimuoverle dal file. Notare che i prototipi non sono presenti
 * nel file es12.h, quindi non sono visibili (e quindi utilizzabili) dal main. */
int rig(int p, int n) {
	return -1;
}

int col(int p, int n) {
	return -1;
}

int linear(int r, int c, int n) {
	return -1;
}

/* Queste invece è il caso di implementarle! :) */
int quadrato_magico(int m[], int n) {
	return -1;
}

int quadrato_perfetto(int m[], int n) {
	return -1;
}

int crea_quadrato_perfetto(int m[], int n) {
	return -1;
}
